import React from 'react';
import BootStatus from '../components/BootStatus';

export default function BootView() {
  return (
    <div className="view-container boot-view">
      <BootStatus />
    </div>
  );
}
