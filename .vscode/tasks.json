{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"label": "build",
			"command": "go build -v ./...",
			"group": "build",
			"problemMatcher": [
				"$go"
			],
			"isBackground": false
		}
	]
} *  The terminal process "/usr/bin/bash '-c', 'go build -v ./...'" failed to launch (exit code: 1). 
 *  Terminal will be reused by tasks, press any key to close it. 

 *  Executing task: go build -v ./... 

internal/docker/docker.go:6:2: no required module provides package github.com/docker/docker/client; to add it:
        go get github.com/docker/docker/client

 *  The terminal process "/usr/bin/bash '-c', 'go build -v ./...'" failed to launch (exit code: 1). 
 *  Terminal will be reused by tasks, press any key to close it. 


